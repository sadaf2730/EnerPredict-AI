{
  "name": "enerpredict-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EnerPredict AI - Personalized Energy Predictive Insight Dashboard Monorepo for Vercel",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5"
  }
}
