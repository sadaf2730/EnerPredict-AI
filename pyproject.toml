[project]
name = "enerpredict-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "pandas",
  "numpy",
  "scikit-learn",
  "joblib",
  "pydantic"
]